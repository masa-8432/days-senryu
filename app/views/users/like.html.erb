<div class="container">

  <div class="row justify-content-center login">
    <div class="col-6">
      <h2><%= @user.name %>さんがいいねした投稿</h2>
    </div>
  </div>

  <% if @users.count != 0 %>

    <!--いいねした投稿があれば一覧を表示する-->
    <% @users.each do |post| %>
      <!--投稿表示部分-->
      <div class="row">
        <div class="post">
          <%= render 'posts/item', post: post %>
          <div class="sub">
            <%= render 'posts/time', post: post %>
            <%= render partial: 'posts/favorite', locals: {post: post} %>
            <%= render 'posts/comment', post: post %>
          </div>
        </div>
      </div>
    <% end %>

  <% else %>
    <!--いいねした投稿がなければ表示-->
    <div class="no-message-box">
     <p class="text-center">まだありません</p>
    </div>

  <% end %>

  <!--ページネーション-->
  <%= render 'layouts/pager', obj: @users %>

</div>