
  <div class="row">

    <div class="post">

      <div class="main">
        <div class="box">

          <p class="theme">お題 「<%= post.theme %>」</p>
          <div class="text">
              <%= post.text %>
          </div>
        </div>
      </div>

      <div class="sub">
        <%= link_to user_path(post.user) do %>
          <%= post.user.name %></br>
        <% end %>
        <%= post.created_at.strftime("%Y-%m-%d %H:%M") %></br>
        <i class="far fa-heart"></i>&nbsp;
        <%= link_to like_post_path(post.id) do %>
          <%= post.favorites.count %></br>
        <% end %>
        <i class="far fa-comment-alt"></i>&nbsp;
        <%= link_to post_path(post.id) do %>
          <%= post.post_comments.count %>
         <% end %>
      </div>

    </div>
     </div>

